# games.aodhancoyne.com

Index of browser games, linking out to each game's own GitHub Pages deployment.

Static HTML, no build step. Served by GitHub Pages at `games.aodhancoyne.com`
(see `CNAME`).

Adding a game: copy an `<a class="game">` block in `index.html`.
